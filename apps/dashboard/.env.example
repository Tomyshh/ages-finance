# URL du service api-gateway (server-side uniquement)
API_GATEWAY_URL=http://localhost:8080
# Jeton bearer attendu par l'api-gateway (doit matcher GATEWAY_PUBLIC_TOKEN du gateway)
GATEWAY_PUBLIC_TOKEN=
